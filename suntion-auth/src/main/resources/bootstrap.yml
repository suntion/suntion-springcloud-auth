spring:
    application:
        name: suntion-auth
    profiles:
        active: ${active.profile}
    cloud:
        config:
            uri: ${configServer.url}
            label: master
        bootstrap:
            enabled: false
