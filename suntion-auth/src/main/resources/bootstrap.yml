spring:
    application:
        name: suntion-auth
    profiles:
        active: ${active.profile}
    cloud:
        config:
            uri: ${config.url}
            label: master
logging:
    path: /var/log/${spring.application.name}