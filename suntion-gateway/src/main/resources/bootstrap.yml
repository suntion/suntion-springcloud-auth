spring:
  application:
    name: suntion-gateway
  profiles:
    active: ${active.profile}
  cloud:
    config:
      uri: ${configServer.url}
      label: master
    bootstrap:
      enabled=false:
logging:
  path: /var/log/${spring.application.name}