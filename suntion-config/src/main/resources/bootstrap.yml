server:
  port: 8888
spring:
  application:
    name: suntion-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/suntion/suntion-auth-cloud.git
          search-paths: suntion-config