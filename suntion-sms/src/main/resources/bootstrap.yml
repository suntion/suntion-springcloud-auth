spring:
  application:
    name: suntion-sms
  profiles:
    active: ${active.profile}
  cloud:
    config:
      uri: ${config.url}
      label: master
      enabled: true
logging:
  path: /var/log/${spring.application.name}